export default class TestObjectLink {
    private readonly _id: number;
    private readonly _value: string;

    constructor(id: number, value: string) {
        this._id = id;
        this._value = value;
    }
}